# Batch images compress tool

This is a CLI tool to compress multiple images (JPEG or PNG) at once using python

## Usage

```shell
python -m batch-jpeg-compressor [start directory] [end directory]
```
